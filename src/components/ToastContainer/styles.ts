import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  top: 0;
  right: 0;
  padding: 15px;
  overflow: hidden;

  display: flex;
  flex-direction: column;
`;
